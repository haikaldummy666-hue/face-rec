{
  "name": "face-rec-monorepo",
  "version": "1.0.0",
  "description": "Emotion Detection System - Monorepo",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm install"
  }
}
